webapp that allows users to upload art and generate prompts via images and words.

installed:
  django 2.2.5 installed,
  Pillow,
  django-picsum (django wrapper fpr pixabay api)
  python-decouple (protect api key through .env file)
in venv

User uploaded images are stored localy in the static files. In the full implementation this should not 
will the case. Images should be stored in another server such as Asure Blob
